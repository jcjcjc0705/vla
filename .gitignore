# 錄下來的資料集 — 200 集 × 2 相機約 5 GB,只留在伺服器
data/

# 訓練產物
outputs/
checkpoints/
*.ckpt
*.safetensors

# build_scene.py 產生的場景 —— 是產物不是原始碼,兩台各自生成
assets/*.usd

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
ml-env/
__pycache__/

# build_scene.py 產生的程序性紋理 —— 產物,seed 在 task yaml 裡
assets/textures/

# isaac/fetch_assets.sh 從 omx_bridge_image 匯出的副本 —— 產物,不進 git
.image_cache/

# GR00T 的權重(約 10-15 GB)。跟 .image_cache 一樣是產物,而且是 gated 模型,
# 下載一次就留著 —— HF_HOME 指到這裡,容器重建也不用重抓。
.hf_cache/
