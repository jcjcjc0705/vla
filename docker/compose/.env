# Shared by the sim<->real bridge compose. ROS_DOMAIN_ID must match BOTH the real
# follower (omx_arm_image defaults to 1) and Isaac Sim (launch it with the same value).
ROS_DOMAIN_ID=1

# Fixed-rate command output, in Hz.
COMMAND_RATE=50.0
